name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  test-and-build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Unit Tests
        run: pytest

      - name: Build Docker Image
        run: |
          docker build -t flask-app:${{ github.sha }} .


      - name: Scan Docker Image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: flask-app:${{ github.sha }}
          format: table
          exit-code: 1
          ignore-unfixed: true
          severity: CRITICAL,HIGH

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Tag Docker Image
        run: |
          docker tag flask-app:${{ github.sha }} \
          163265929593.dkr.ecr.ap-south-1.amazonaws.com/devops-cicd-project:${{ github.sha }}

      - name: Push Docker Image
        run: |
          docker push \
          163265929593.dkr.ecr.ap-south-1.amazonaws.com/devops-cicd-project:${{ github.sha }}

      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to EC2
        run: |
          ssh ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << EOF

          aws ecr get-login-password --region ${{ secrets.AWS_REGION }} | \
          docker login --username AWS --password-stdin \
          163265929593.dkr.ecr.ap-south-1.amazonaws.com

          docker pull 163265929593.dkr.ecr.ap-south-1.amazonaws.com/devops-cicd-project:${{ github.sha }}

          docker stop flask-app || true
          docker rm flask-app || true

          docker run -d \
            --name flask-app \
            -p 80:5000 \
            --restart unless-stopped \
            163265929593.dkr.ecr.ap-south-1.amazonaws.com/devops-cicd-project:${{ github.sha }}

          EOF


  